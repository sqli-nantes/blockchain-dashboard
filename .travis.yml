sudo: required
dist: trusty
language: node_js

node_js:
  - "4.2"

cache:
  directories:
    - node_modules

install:
  - npm i -g npm@2;
  - npm install -g webpack
  - npm install

script:
- npm run test && npm run build